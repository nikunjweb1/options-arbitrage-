# Phase 2 (data collection) dependencies only.
# No trading/execution/broker-order libraries added until Phase 8, and even then
# LIVE_TRADING stays False by default per config/settings.py.

requests>=2.31.0
websocket-client>=1.7.0
python-dotenv>=1.0.0

# Shark Exchange uses Socket.IO (Engine.IO v4) for its WebSocket feeds, unlike
# Delta's plain WebSocket -- see exchange_adapters/shark_ws_capture.py. This
# handles the Engine.IO handshake/sid negotiation correctly; do not hand-roll
# that protocol layer.
python-socketio[client]>=5.11.0

# Data handling
pandas>=2.1.0

# Pricing/EV engine (Phase 5) — included early since schemas reference Decimal-heavy math
numpy>=1.26.0
scipy>=1.11.0

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0

# Lint/type-check (dev)
mypy>=1.7.0
ruff>=0.1.9
